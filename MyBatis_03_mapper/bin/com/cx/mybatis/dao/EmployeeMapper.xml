<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	namespace:名称空间;指定为借口的全类名
	id:唯一标识
	resultType:返回值类型
	#{id}:从传递过来的参数中取出id
	
	public Employee getEmpById(Integer id);
 -->
<mapper namespace="com.cx.mybatis.dao.EmployeeMapper">
	
	<select id="getEmpById" resultType="com.cx.mybatis.bean.Employee" databaseId="mysql">
		select id,last_name,email,gender from tbl_employee where id =
		#{id}
	</select>
	<!-- 
		parameterType:可以省略
	 -->
	<insert id="addEmployee" parameterType="com.cx.mybatis.bean.Employee" databaseId="mysql">
		insert into tbl_employee(last_name,email,gender) values(#{lastName},#{email},#{gender})
	</insert>
	
	<update id="updateEmployee">
		update tbl_employee 
		set last_name = #{lastName},email = #{email},gender = #{gender} 
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from tbl_employee where id=#{id}
	</delete>
</mapper>